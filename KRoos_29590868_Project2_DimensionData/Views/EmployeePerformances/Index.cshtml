@model IEnumerable<KRoos_29590868_Project2_DimensionData.Models.EmployeePerformance>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/_ViewImports.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PerformanceRating)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WorkLifeBalance)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JobInvolvement)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PerformanceRating)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WorkLifeBalance)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JobInvolvement)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.empPerformanceID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.empPerformanceID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.empPerformanceID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
